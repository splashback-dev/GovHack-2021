import React from 'react';
import {Button, Typography} from 'antd';

export const Home: React.FC<any> = () => {
    return (
        <div className="h-full w-full flex justify-center items-center">
            <Typography.Title>FROGLY</Typography.Title>
        </div>
    );
};
